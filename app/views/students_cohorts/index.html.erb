<%= link_to "Create Student/Cohort", new_students_cohort_path %>

<% @students_cohort.each do |s| %>
<div id="killer<%= s.id %>">
<div class="container">
<table style= "width: 100%">

    <tr>
        <th>Students</th>
        <th>Cohorts</th>
    </tr>
    <tr>
        <td><%= s.student_id %></td>
        <td><%= s.cohort_id  %></td>
        <td><%= link_to "Edit", edit_students_cohort_path(s.id) %></td><br>
        <td><%= link_to "delete", students_cohort_path(s.id), method: :delete, remote: true %></td>
    </tr>
</table>
</div>
</div>
<% end %>