const item<%= @course.id %> = document.querySelector("#course" + <%= @course.id %>);
let deleteone = document..getElementById('button_<%=@course.id%>')
item<%= @course.id %>.remove();
item.style.display="none"
delete_buttonone.display="none"
